- if products.present?
  - products.each do |product|
    - product.images.each do |image|
      = link_to product_path(product.id) ,class: "my-page__users-posts__post__show" do
        = image_tag image.image.url , class: "users-post-image"
        -# - if image.file.content_type.include?('image/')
        -#   = image_tag product.image.url, width: "100%", height: "100%", class: "users-post-image"
        -# - else
        -#   = video_tag image.url, width: "100%", height: "100%", class: "users-post-image", |
        -#     controls: true, autobuffer: true

- else
  まだ該当する商品はありません。