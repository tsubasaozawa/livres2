= render 'shared/header'
Hello world

= link_to new_product_path do
  投稿する

- @products.each do |product|
  = link_to product_path(product.id), class: "" do
    ¥#{product.price.to_s.gsub(/(\d)(?=\d{3}+$)/, '\\1,')}
    - product.images.each do |img|
      = image_tag(img.image.url, class:"")
    = product.title